Q1) select machine_id, month(maintenance_date) from l1_maintenance_records where cost > (select avg(cost) from l1_maintenance_records ) and month(maintenance_date)<4;

Q2) select maintenance_type , count(maintenance_type) from l1_maintenance_records where month(maintenance_date)=3 group by maintenance_type;

Q3) select technician_name, count(technician_name) ,avg(cost) from l1_maintenance_records where cost > (select avg(cost) from l1_maintenance_records ) and month(maintenance_date)=3 group by technician_name;

Q4) select round(avg(incident_count_pre_update-incident_count_post_update),2) as avg_reduction  from l2_updates group by product_id;

Q5) select update_id,user_feedback_score from l2_updates order by user_feedback_score desc limit 1;

Q6) select product_id, avg(incident_count_pre_update-incident_count_post_update) from l2_updates group by product_id order by product_id limit 1;

Q7) select patient_name, count(patient_id) from l3_patients join l3_visits using(patient_id) join L3_treatments using (visit_id) group by patient_name;

Q8) select facility_name, count(facility_name) as count_of_visit from l3_facilities join l3_visits using(facility_id) where reason_for_visit="Emergency" group by facility_name;

Q9) select insurance_provider from l3_patients join l3_visits using(patient_id) join l3_treatments using(visit_id) where outcome="Complicated" ;

Q10) select product_name,category,quantity from l4_products join l4_sales using(product_id) where quantity >( select avg(quantity) from l4_sales group by category)  ;

Q11) create view view_data as select Region, (price*quantity) as total_sales from l4_products join l4_sales using(product_id) join l4_stores using(store_id) order by region desc limit 1 ;

Q12) select Region, product_name from l4_products join l4_sales using(product_id) join l4_stores using(store_id) order by region ;
